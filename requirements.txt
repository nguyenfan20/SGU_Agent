Flask>=2.3.2
pandas>=2.0.3
numpy>=1.24.3
faiss-cpu>=1.7.4
# sentence-transformers # No longer needed
# torch # No longer needed (unless other libs implicitly need it, but openai/faiss-cpu usually don't)
# transformers # No longer needed
# sentencepiece # No longer needed
openai>=1.0.0
python-dotenv>=1.0.0 # For loading .env file